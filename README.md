# EcoPolice-Server
Server for EcoPolice app

### Run
`node index.js`  
or  
```bash
npm i pm2 -g
pm2 start index.js --watch
```

#### Methods
* `GET /all`
* `GET /one/<id>`
* `POST /add`
* `POST /upload`

